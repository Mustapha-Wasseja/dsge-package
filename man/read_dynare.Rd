% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-dynare.R
\name{read_dynare}
\alias{read_dynare}
\title{Import a Dynare .mod File}
\usage{
read_dynare(file, text = NULL, observed = NULL)
}
\arguments{
\item{file}{Path to a \code{.mod} file.}

\item{text}{Alternatively, the model code as a character vector (one
element per line, or a single string). Used instead of \code{file} when
supplied.}

\item{observed}{Optional character vector of observed variables. Overrides
the file's \code{varobs} declaration. dsge requires as many observed
variables as shocks; see Details.}
}
\value{
An object of class \code{"dsge_dynare"}, a list with components:
\describe{
\item{model}{The translated \code{dsgenl_model}. Parameters listed in
\code{estimated_params} are free (with starting values); all other
parameters are fixed at their calibrated values.}
\item{params}{Named numeric vector of calibrated parameter values.}
\item{shock_sd}{Named numeric vector of shock standard deviations from
the \code{shocks} block (0 for shocks with no declared variance, as in
Dynare).}
\item{priors}{Named list of \code{\link[=prior]{prior()}} objects translated from
\code{estimated_params}, ready for \code{\link[=bayes_dsge]{bayes_dsge()}}, or \code{NULL}.}
\item{estimated_params}{Data frame describing each \code{estimated_params}
entry and how it was translated.}
\item{observed}{Observed variables used for the model.}
\item{variables, shocks, parameters}{Names declared in the file.}
\item{aux}{Data frame of auxiliary variables created for leads and
lags.}
\item{commands}{List of Dynare commands found in the file (such as
\code{stoch_simul} or \code{estimation}), recorded but not executed.}
\item{notes}{Character vector of translation notes, including
anything that was ignored or approximated.}
}
}
\description{
Reads a Dynare model file and translates it into a nonlinear dsge
model, together with the calibration, shock standard deviations and
priors it declares, so that the model can be solved, simulated or
estimated in R without Dynare, MATLAB or Octave.
}
\details{
\strong{Supported:} \code{var}, \code{varexo}, \code{parameters},
\code{predetermined_variables}, \code{varobs}, parameter assignments, the
\code{model} block (including \code{model(linear)}, equation tags and \verb{#}
model-local variables), leads and lags of any length, \code{initval},
\code{steady_state_model}, the variance part of the \code{shocks} block, and
\code{estimated_params} / \code{estimated_params_init}.

\strong{Timing.} No manual re-timing is needed. Every Dynare variable
becomes a control; lags become auxiliary state variables named
\code{x_lag1}, \code{x_lag2}, ...; leads beyond one period become auxiliary
controls \code{x_lead1}, ...; and each shock becomes an exogenous state that
holds the current innovation. Impulse responses therefore have the
same timing as in Dynare. The auxiliary variables also appear in
solution and IRF output.

\strong{Observed variables.} dsge requires the number of observed variables
to equal the number of shocks. If \code{varobs} (or \code{observed}) has that
many variables it is used as is; otherwise, and when no \code{varobs} is
given, the first declared endogenous variables are used and a note is
recorded. The choice only matters for estimation.

\strong{Priors.} Dynare's mean/standard-deviation prior parameterisation is
converted to dsge's: \code{normal_pdf}, \code{beta_pdf}, \code{gamma_pdf},
\code{uniform_pdf} and \code{inv_gamma2_pdf} are translated exactly.
\code{inv_gamma_pdf} / \code{inv_gamma1_pdf} (a prior on a standard deviation) has
no exact dsge counterpart and is approximated by an inverse gamma with
the same mean and standard deviation (an infinite standard deviation
maps to shape 2). Shifted or generalised priors and \code{weibull_pdf} are
not translated and are listed in \code{notes}.

\strong{Not supported:} macro-processor directives (\verb{@#}; expand them first
with \verb{dynare model.mod savemacro onlymacro} and import the generated
\code{model-macroexp.mod}), \code{varexo_det}, \code{external_function},
\code{STEADY_STATE()}, \code{EXPECTATION()}, \code{diff()}, \code{adl()}, PAC and VAR
expectation operators, and correlated shocks (correlations are listed
in \code{notes} and ignored). Other blocks and commands are recorded but not
run.
}
\examples{
rbc <- read_dynare(system.file("examples", "rbc.mod", package = "dsge"))
rbc
sol <- solve_dsge(rbc)
irf(sol, periods = 20, se = FALSE)

# Model code can also be passed as text
ar1 <- read_dynare(text = "
  var y;
  varexo e;
  parameters rho;
  rho = 0.9;
  model;
    y = rho * y(-1) + e;
  end;
  shocks;
    var e; stderr 0.01;
  end;
")
solve_dsge(ar1)

}
\seealso{
\code{\link[=dsgenl_model]{dsgenl_model()}}, \code{\link[=solve_dsge]{solve_dsge()}}, \code{\link[=bayes_dsge]{bayes_dsge()}}
}

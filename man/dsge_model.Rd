% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-specification.R
\name{dsge_model}
\alias{dsge_model}
\title{Define a Linear DSGE Model}
\usage{
dsge_model(..., fixed = list(), start = list(), derived = NULL)
}
\arguments{
\item{...}{Equation specifications created by \code{\link[=obs]{obs()}}, \code{\link[=unobs]{unobs()}},
and \code{\link[=state]{state()}}.}

\item{fixed}{Named list of parameter values to hold fixed
(constrained) during estimation.}

\item{start}{Named list of starting values for free parameters.}

\item{derived}{Optional function mapping a named list of primitive
parameter values to a named list of derived parameter values
(e.g. \code{beta_bar = beta * gamma^(-sigma_c)}).  Called by
\code{\link[=solve_dsge]{solve_dsge()}} at every solve so derived parameters track the
current primitives during estimation.  The returned list is
concatenated with the primitives before equation coefficients are
evaluated.  Names returned by \code{derived()} may then appear directly
inside equation coefficients alongside primitive parameters.}
}
\value{
An object of class \code{"dsge_model"} containing:
\describe{
\item{equations}{List of parsed equation objects.}
\item{variables}{List with elements \code{observed}, \code{unobserved},
\code{exo_state}, \code{endo_state}.}
\item{parameters}{Character vector of all parameter names.}
\item{free_parameters}{Character vector of free (estimable) parameter names.}
\item{fixed}{Named list of fixed parameter values.}
\item{start}{Named list of starting values.}
}
}
\description{
Constructs a linear DSGE model object from a set of equations.
Each equation is wrapped in \code{\link[=obs]{obs()}}, \code{\link[=unobs]{unobs()}}, or \code{\link[=state]{state()}} to
indicate the role of the left-hand-side variable.
}
\details{
A linear DSGE model is specified as a system of equations in which
variables enter linearly but parameters may enter nonlinearly.

Use \code{lead(x)} or \code{E(x)} within formulas to denote the one-period-ahead
model-consistent expectation of variable \code{x}.

The number of exogenous state variables (those with shocks) must equal
the number of observed control variables.
}
\examples{
# Simple New Keynesian model
nk <- dsge_model(
  obs(p   ~ beta * lead(p) + kappa * x),
  unobs(x ~ lead(x) - (r - lead(p) - g)),
  obs(r   ~ psi * p + u),
  state(u ~ rhou * u),
  state(g ~ rhog * g),
  fixed = list(beta = 0.96),
  start = list(kappa = 0.1, psi = 1.5, rhou = 0.7, rhog = 0.9)
)

}

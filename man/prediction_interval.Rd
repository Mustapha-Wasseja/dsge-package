% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction-tools.R
\name{prediction_interval}
\alias{prediction_interval}
\title{Prediction intervals for DSGE models}
\usage{
prediction_interval(object, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{"dsge_fit"} object.}

\item{level}{Confidence level for prediction intervals (default 0.95).}

\item{...}{Additional arguments passed to \code{predict()}.}
}
\value{
An object of class \code{"dsge_prediction_interval"} with
components \code{fit}, \code{lower}, \code{upper}, \code{se},
\code{level}, and \code{variables}.
}
\description{
Computes point predictions and prediction intervals using the
one-step-ahead innovation variance from the Kalman filter.
}
\examples{
\donttest{
  m <- dsge_model(
    obs(y ~ z),
    state(z ~ rho * z),
    start = list(rho = 0.5)
  )
  set.seed(1)
  z <- numeric(100); for (i in 2:100) z[i] <- 0.8 * z[i-1] + rnorm(1)
  fit <- estimate(m, data = data.frame(y = z))
  pi <- prediction_interval(fit, level = 0.95)
  print(pi)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynare-perfect-foresight.R
\name{simulate_perfect_foresight}
\alias{simulate_perfect_foresight}
\title{Perfect-foresight simulation of an imported Dynare model}
\usage{
simulate_perfect_foresight(
  x,
  periods = NULL,
  shocks = NULL,
  params = NULL,
  lmmcp = NULL,
  tol = 1e-10,
  max_iter = 100L
)
}
\arguments{
\item{x}{A model imported with \code{\link[=read_dynare]{read_dynare()}}.}

\item{periods}{Number of simulation periods. Defaults to the file's
\code{perfect_foresight_setup(periods = )}, \code{simul(periods = )} or
\code{periods} statement.}

\item{shocks}{Optional matrix (or data frame) of exogenous values by
period (rows 1, 2, ...) with columns named after shocks, replacing the
file's deterministic shocks for those shocks.}

\item{params}{Optional named parameter values overriding the calibration.}

\item{lmmcp}{Logical: treat equations tagged \code{mcp} as complementarity
conditions (Dynare's \code{lmmcp} option). Defaults to \code{TRUE} when a
\code{perfect_foresight_solver} command of the file uses \code{lmmcp}; without
it Dynare ignores the tags.}

\item{tol}{Convergence tolerance on the largest equation residual.}

\item{max_iter}{Maximum number of Newton iterations.}
}
\value{
An object of class \code{"dsge_dynare_pf"}: a list with \code{path} (a
matrix of the endogenous variables, one row per period from the
initial condition(s) to the terminal one(s), as Dynare's
\code{oo_.endo_simul} transposed), \code{exo} (the exogenous path, as
\code{oo_.exo_simul}), \code{periods}, \code{initial} and \code{terminal} (steady states
or initial/terminal values), \code{converged}, \code{iterations} and
\code{max_residual}.
}
\description{
Solves the model's deterministic path exactly as Dynare's
\code{perfect_foresight_setup} + \code{perfect_foresight_solver} (or \code{simul}) do,
using the file's \code{initval}, \code{endval}, \code{histval} and \code{steady} statements
and its deterministic \code{shocks} (with \code{periods}/\code{values}), and
complementarity conditions from \code{mcp} equation tags (Dynare's \code{lmmcp}
option, e.g. a zero lower bound).
}
\examples{
m <- read_dynare(text = "
  var c k;
  varexo a;
  parameters alpha beta delta;
  alpha = 0.33; beta = 0.99; delta = 0.025;
  model;
    1/c = beta/c(+1) * (alpha * exp(a(+1)) * k^(alpha - 1) + 1 - delta);
    k = exp(a) * k(-1)^alpha + (1 - delta) * k(-1) - c;
  end;
  initval; a = 0; k = 30; c = 2.3; end;
  steady;
  endval; a = 0.1; end;
  steady;
  perfect_foresight_setup(periods = 100);
  perfect_foresight_solver;
")
pf <- simulate_perfect_foresight(m)
head(pf$path)
}

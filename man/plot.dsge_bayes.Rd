% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes-methods.R
\name{plot.dsge_bayes}
\alias{plot.dsge_bayes}
\title{Plot Bayesian DSGE Results}
\usage{
\method{plot}{dsge_bayes}(
  x,
  type = c("trace", "density", "prior_posterior", "running_mean", "acf", "pairs", "all",
    "irf"),
  pars = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{dsge_bayes} object from \code{\link[=bayes_dsge]{bayes_dsge()}}.}

\item{type}{Character. Plot type:
\describe{
\item{\code{"trace"}}{Trace plots showing MCMC chains for each parameter.
All chains are overlaid with distinct colors. Useful for assessing
convergence and mixing.}
\item{\code{"density"}}{Posterior density plots for each parameter with
the prior distribution overlaid as a red dashed line. Useful for
seeing how much the data updated the prior.}
\item{\code{"prior_posterior"}}{Dedicated prior-vs-posterior comparison.
Same layout as \code{"density"} but with the title
"Prior vs Posterior" to emphasize the comparison.}
\item{\code{"running_mean"}}{Cumulative posterior mean by iteration for
each parameter. All chains are shown. Useful for assessing whether
the chain has settled.}
\item{\code{"acf"}}{Autocorrelation function plots for each parameter,
pooled across chains. Useful for diagnosing slow mixing.}
\item{\code{"pairs"}}{Pairwise scatter plots of posterior draws (lower
triangle) with correlation coefficients (upper triangle) and
marginal histograms (diagonal). Useful for detecting parameter
correlations. Draws are thinned for readability.}
\item{\code{"all"}}{A combined diagnostic panel showing trace plot (left)
and posterior density with prior (right) side by side for each
parameter.}
\item{\code{"irf"}}{Posterior impulse-response functions with credible
bands. Calls \code{\link[=irf]{irf()}} internally and plots the result. Additional
arguments \code{periods}, \code{impulse}, \code{response}, \code{n_draws}, and \code{level}
are passed through.}
}}

\item{pars}{Character vector of parameter names to include. If \code{NULL}
(default), all parameters are shown. Applies to \code{trace}, \code{density},
\code{prior_posterior}, \code{running_mean}, \code{acf}, \code{pairs}, and \code{all}.
Ignored for \code{irf} (use \code{impulse}/\code{response} instead).}

\item{...}{Additional arguments. For \code{type = "irf"}, arguments
\code{periods}, \code{impulse}, \code{response}, \code{n_draws}, and \code{level} are
passed to \code{\link[=irf.dsge_bayes]{irf.dsge_bayes()}}.}
}
\value{
Invisibly returns the \code{dsge_bayes} object \code{x}. Called for the
side effect of producing diagnostic plots on the active graphics device.
}
\description{
Produces diagnostic plots for posterior draws from a Bayesian DSGE fit.
}
\details{
All plot types except \code{pairs} and \code{irf} handle any number of parameters
by paginating across multiple plot pages (up to 4 parameters per page).
In interactive sessions, \code{devAskNewPage()} is used to prompt between pages.

For the \code{"pairs"} plot, at most 1000 draws are used to keep the plot
readable. The correlation matrix is printed to the console.

\strong{Forecast plotting} is not currently supported for Bayesian fits.
Use \code{\link[=irf]{irf()}} for posterior impulse-response analysis.
}
\examples{
\donttest{
m <- dsge_model(
  obs(y ~ z),
  state(z ~ rho * z),
  start = list(rho = 0.5)
)
set.seed(42)
z <- numeric(200); for (i in 2:200) z[i] <- 0.8 * z[i-1] + rnorm(1)
fit <- bayes_dsge(m, data = data.frame(y = z),
                  priors = list(rho = prior("beta", shape1 = 2, shape2 = 2)),
                  chains = 2, iter = 2000, seed = 1)
plot(fit, type = "trace")
plot(fit, type = "density")
plot(fit, type = "prior_posterior")
plot(fit, type = "running_mean")
plot(fit, type = "acf")
plot(fit, type = "all")
# Parameter selection
plot(fit, type = "trace", pars = "rho")
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/particle-filter.R
\name{particle_filter_loglik}
\alias{particle_filter_loglik}
\title{Particle Filter Log-Likelihood for a DSGE Solution}
\usage{
particle_filter_loglik(
  sol,
  y,
  n_particles = 1000L,
  meas_sd = 0.001,
  seed = NULL
)
}
\arguments{
\item{sol}{A \code{dsge_solution} object from \code{\link{solve_dsge}}.}

\item{y}{Matrix or data frame of observed data (T x n_obs).}

\item{n_particles}{Integer.  Number of particles.  Default 1000.}

\item{meas_sd}{Numeric.  Measurement error standard deviation.
Default 0.001.}

\item{seed}{Optional integer random seed.}
}
\value{
Scalar log-likelihood estimate.
}
\description{
Convenience wrapper around \code{\link{particle_filter}} that accepts a
\code{dsge_solution} object and observed data directly.
}
\examples{
\donttest{
m <- dsge_model(
  obs(y ~ z), state(z ~ rho * z), start = list(rho = 0.8)
)
set.seed(1)
z <- numeric(100); for (i in 2:100) z[i] <- 0.8 * z[i-1] + rnorm(1)
dat <- data.frame(y = z - mean(z))
sol <- solve_dsge(m, params = c(rho = 0.8), shock_sd = c(z = 0.2))
ll  <- particle_filter_loglik(sol, dat, n_particles = 500, seed = 1)
}

}
\seealso{
\code{\link{particle_filter}}, \code{\link{bayes_particle}}
}

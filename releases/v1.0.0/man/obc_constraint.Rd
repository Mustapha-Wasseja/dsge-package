% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occbin.R
\name{obc_constraint}
\alias{obc_constraint}
\title{Create an Occasionally Binding Constraint}
\usage{
obc_constraint(variable, type = ">=", bound = 0, shock = NULL)
}
\arguments{
\item{variable}{Character. Name of the constrained variable (must be a
control variable in the model).}

\item{type}{Character. Either \code{">="} or \code{"<="}.}

\item{bound}{Numeric. The constraint bound. For models with steady state,
this is in levels; for linear models, in deviations.}

\item{shock}{Character or \code{NULL}. Name of the shock channel used to
enforce the constraint. If \code{NULL} (default), auto-detected as the
shock with the largest impact on the constrained variable.}
}
\value{
An \code{obc_constraint} object.
}
\description{
Specifies an inequality constraint for use with \code{simulate_occbin}.
}
\examples{
# Zero lower bound on nominal interest rate
obc_constraint("r", ">=", 0)

# Upper bound on debt ratio
obc_constraint("b", "<=", 0.6)

}

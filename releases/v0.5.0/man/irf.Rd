% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes-irf.R, R/irf.R
\name{irf.dsge_bayes}
\alias{irf.dsge_bayes}
\alias{irf}
\title{Compute Impulse-Response Functions}
\usage{
\method{irf}{dsge_bayes}(
  x,
  periods = 20L,
  impulse = NULL,
  response = NULL,
  se = TRUE,
  level = 0.95,
  n_draws = 200L,
  ...
)

irf(
  x,
  periods = 20L,
  impulse = NULL,
  response = NULL,
  se = TRUE,
  level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{dsge_fit} or \code{dsge_solution} object.}

\item{periods}{Integer. Number of periods to compute. Default is 20.}

\item{impulse}{Character vector of shock names. If \code{NULL} (default),
computes IRFs for all shocks.}

\item{response}{Character vector of variable names. If \code{NULL} (default),
computes responses for all variables.}

\item{se}{Logical. If \code{TRUE} (default) and \code{x} is a \code{dsge_fit},
compute delta-method standard errors for IRFs.}

\item{level}{Confidence level for bands. Default is 0.95.}

\item{n_draws}{Integer. Number of posterior draws to use for IRF
computation. Default is 200. Set to \code{NULL} to use all draws.}

\item{...}{Additional arguments passed to methods.}
}
\value{
An object of class \code{"dsge_irf"} containing a data frame with
columns: \code{period}, \code{impulse}, \code{response}, \code{value}, and optionally
\code{se}, \code{lower}, \code{upper}.
}
\description{
Computes the impulse-response functions (IRFs) from a fitted or solved
DSGE model. An IRF traces the dynamic response of control and state
variables to a one-standard-deviation shock.
}
\section{Methods (by class)}{
\itemize{
\item \code{irf(dsge_bayes)}: Compute posterior IRFs from a Bayesian DSGE fit.
Returns pointwise posterior median and credible bands.

}}
\examples{
\donttest{
m <- dsge_model(
  obs(y ~ z),
  state(z ~ rho * z),
  start = list(rho = 0.5)
)
sol <- solve_dsge(m, params = c(rho = 0.8))
irfs <- irf(sol, periods = 10)
}

}
